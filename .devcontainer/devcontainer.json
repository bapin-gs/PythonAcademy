{
  "name": "Python Data Engineering Trainings",
  "dockerComposeFile": "docker-compose.yml",
  "service": "workspace",
  "workspaceFolder": "/workspaces/PythonAcademy",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-toolsai.jupyter",
        "mtxr.sqltools",
        "mtxr.sqltools-driver-pg",
        "github.copilot",
        "github.copilot-chat"
      ]
    }
  },
  "forwardPorts": [8888, 5432],
  "postCreateCommand": "pip install --upgrade pip && pip install jupyterlab ipykernel pandas pyarrow sqlalchemy psycopg2-binary asyncpg python-dotenv black ruff && python -m ipykernel install --user --name trainings --display-name 'Python (trainings)'",
  "remoteEnv": {
    "POSTGRES_HOST": "db",
    "POSTGRES_PORT": "5432",
    "POSTGRES_DB": "trainings",
    "POSTGRES_USER": "trainings",
    "POSTGRES_PASSWORD": "trainings"
  }
}
